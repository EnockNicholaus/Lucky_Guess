# the combine module produces all kinds of passwods i.e (numeral, alphabetical, alpha_numeral) 
#.
# it produces the largest amount of passwords compared to the other modules
#.
# also uses random words in the password generation from the words.txt file in the common folder